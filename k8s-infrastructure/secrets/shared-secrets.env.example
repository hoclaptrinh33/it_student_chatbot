# Copy to shared-secrets.env (gitignored) before `kubectl apply -k`.
# Do not put real tokens in git.
CLOUDFLARED_TOKEN=replace-with-cloudflare-tunnel-token
JWT_SECRET_KEY=replace-with-jwt-secret
MONGODB_URL=mongodb://mongodb:27017
